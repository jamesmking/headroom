import type {Metadata} from 'next';

export const metadata: Metadata = {
  title: 'Headroom | Settings',
};

const Tickets = () => {
  return (
    <div>
      <h1>Settings</h1>
    </div>
  );
};

export default Tickets;
